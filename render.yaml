databases:
  - name: anon-chat-db
    databaseName: anon_chat
    user: anon_user
    plan: basic-1gb # Cost-efficient (~$20/mo) for a TG Mini App
    postgresMajorVersion: "15"
    ipAllowList:
      - source: 0.0.0.0/0
        description: everywhere

services:
  # 1. Redis Cache
  - type: keyvalue
    name: anon-chat-redis
    plan: standard
    ipAllowList:
      - source: 0.0.0.0/0
        description: everywhere

  # 2. FastAPI Backend Service
  - type: web
    name: anon-chat-backend
    plan: standard
    runtime: docker
    rootDir: backend
    dockerfilePath: Dockerfile
    region: frankfurt
    numInstances: 3  
    envVars:
      - key: BOT_TOKEN
        sync: false # Render will pause the deploy and ask you to type this in
      - key: WEBAPP_URL
        value: https://anon-chat-frontend.onrender.com # Hardcoded to prevent circular dependency
      - key: DATABASE_URL
        fromDatabase:
          name: anon-chat-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: anon-chat-redis
          property: connectionString
      - key: CENTRIFUGO_URL
        value: http://anon-chat-centrifugo:8000
      - key: CENTRIFUGO_API_KEY
        generateValue: true
      - key: CENTRIFUGO_HMAC_SECRET
        generateValue: true

  # 3. Centrifugo Service (WebSocket Engine)
  - type: web
    name: anon-chat-centrifugo
    plan: standard
    runtime: docker
    dockerfilePath: ./Dockerfile.centrifugo
    region: frankfurt
    envVars:
      - key: PORT
        value: "8000"
      - key: CENTRIFUGO_ADMIN_PASSWORD
        generateValue: true
      - key: CENTRIFUGO_ADMIN_SECRET
        generateValue: true
      - key: CENTRIFUGO_API_KEY
        fromService:
          type: web
          name: anon-chat-backend
          envVarKey: CENTRIFUGO_API_KEY
      - key: CENTRIFUGO_TOKEN_HMAC_SECRET_KEY
        fromService:
          type: web
          name: anon-chat-backend
          envVarKey: CENTRIFUGO_HMAC_SECRET
      - key: CENTRIFUGO_ALLOWED_ORIGINS
        value: "*"

  # 4. Vue/Vite Frontend (Static Web App)
  - type: web
    env: static # Official spec for static sites
    name: anon-chat-frontend
    rootDir: frontend
    buildCommand: npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://anon-chat-backend.onrender.com # Hardcoded for https:// prefix
      - key: VITE_CENTRIFUGO_URL
        value: https://anon-chat-centrifugo.onrender.com # Hardcoded for https:// prefix
